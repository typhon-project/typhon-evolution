import /src/schema.xmi ;

changeOperators [ 
	rename relation orders as commandes
	change cardinality commandes as 0..*
	remove relation commandes
]

from User u, Order o select o where u.name == "jerome", u.orders == o,

from Order p select p,

from User p select u.comments
