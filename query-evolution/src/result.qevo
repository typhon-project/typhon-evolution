changeOperators [ 
	rename entity Product as Item 
	//rename attribute prod as product_id
	merge entities Item Stock as Product_info
]

// Insert queries here


from Product_info p select p where p.id == "1230",

from Product_info p, Stock p select p, p where p.id == "30",

from Person p select p.name,

from Product_info s, Person p select p, s where s.customer == p,

from Product_info p, Stock p select p, p.quantity where p.id == r.reviewed_product, p.prod == p.id  