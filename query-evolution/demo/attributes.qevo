import /src/schema.xmi;

changeOperators [ 
	rename attribute name from User as firstname
]

from User u, Order o select o where u.orders == o, u.name == "jerome" ,

from Order o select o,

from User u select u.name